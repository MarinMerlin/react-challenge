# React Challenge: Recommendation carousel

To setup, have npm installed and run :

    npm install

Thats it! Now you can run it with:

    npm start

You can launch a very basic test suite (just test if the app component renders correctly) with:

    npm test

